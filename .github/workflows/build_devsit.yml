name: Build Devsit
on:
  workflow_dispatch:

jobs:
  build_app:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        include:
          - appname: dev1
            env: dev_env1
            db_connection: dev_db1
            sit_appname: sit1
            sit_env: sit_env1
            sit_db_connection: sit_db1

          - appname: dev2
            env: dev_env2
            db_connection: dev_db2
            sit_appname: sit2
            sit_env: sit_env2
            sit_db_connection: sit_db2

    outputs:
      artifact_name: ${{ steps.define_artifact.outputs.artifact_name }}

    steps:  
      - name: Checkout Code 
        uses: actions/checkout@v4

      - name: Install .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0'

      - name: Build Dotnet
        run: dotnet build -c Release
    
      - name: Publish Dotnet
        run: dotnet publish -c Release -o ./publish_output

      - name: Define artifact
        id: define_artifact
        run: |
          ATF_NAME="Dotnet artifact"
          echo "artifact_name=$ATF_NAME" >> $GITHUB_OUTPUT # Pass value to $GITHUB_OUTPUT

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.define_artifact.outputs.artifact_name }}
          path: ./publish_output

  pass_inputs:
    needs: build_app
    uses: ./.github/workflows/deploy_devsit.yml
    with:
      appname: ${{ inputs.appname }}
      env: ${{ inputs.env }}
      db_connection: ${{ inputs.db_connection }}
      artifact_name: ${{ needs.build_app.outputs.artifact_name }}
      sit_appname: ${{ inputs.appname }}
      sit_env: ${{ inputs.env }}
      sit_db_connection: ${{ inputs.db_connection }}


