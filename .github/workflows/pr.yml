name: Pull request workflow

on:
  pull_request:
    branches: 
      - main


jobs:
  build:
    name: Test checkout
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - run: |
          ls Box
          echo "========================"
          cat Box/hw1.md

      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref:
      
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}