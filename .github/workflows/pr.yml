name: Run on PR
on:
  pull_request:
    branches:
      - main
  
jobs:
  build:
    name: Test checkout
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - run: |
          ls Box