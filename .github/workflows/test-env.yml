name: Test environment

on:
  workflow_dispatch:

jobs:
  dev: &template 
    runs-on: ubuntu-latest
    environment: E1

    steps:
      - run: echo ${{ vars.V1 }}

  sit: 
    <<: *template
    needs: dev
    environment: E2

  prod: 
    <<: *template
    needs: dev
    environment: E3

